const api = 'http://localhost:8000/api';

export const GET_USER             = `${api}/user`;
export const GET_USERS_BY_EMAILS  = `${api}/users-by-emails`;
export const GET_MESSAGES         = `${api}/messages`;
export const OAUTH                = `${api}/oauth`;
export const AUTH0_CREDS          = `${api}/auth0-credentials`;
export const POST_MESSAGE         = `${api}/conversation`;